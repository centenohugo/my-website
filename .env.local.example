POSTGRES_URL=
BLOB_READ_WRITE_TOKEN=
ADMIN_PASSWORD=

# Canonical origin used for canonical URLs, sitemap.xml, robots.txt, llms.txt
# and JSON-LD. Leave empty on Vercel to fall back to the production deployment
# URL; set it once a custom domain is in place (no trailing slash).
NEXT_PUBLIC_SITE_URL=

# Spanish translation (see lib/translate.ts)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
